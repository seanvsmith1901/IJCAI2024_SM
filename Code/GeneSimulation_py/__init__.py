# This file is just to allow for relative path imports. Don't delete, but do go yell at the python devs for a bad
#  (A.K.A I probably just don't fully understand it) import system

import os
import sys

sys.path.append(os.path.join(os.path.dirname(__file__), ".."))